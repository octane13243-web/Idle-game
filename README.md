# Idle-game

A simple browser idle game called **Cookie Reactor Idle**.

## Run

Open `index.html` in your browser.

## Features

- Manual cookie clicking
- Auto-generators with scaling costs
- Achievements that grant click power bonuses
- Save / load from browser localStorage
